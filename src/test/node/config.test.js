
import {expect, assert} from 'chai'

describe('config', () => {

  const CMS = require('../../lib/node/cms')

  it('should read from a file and give the content to the browser', () => {
    assert(CMS)
    
  })
})